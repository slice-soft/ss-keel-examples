# Application
app.name=${SERVICE_NAME:gorm-postgres}
app.env=${APP_ENV:development}
server.port=${PORT:7331}

# API docs
docs.path=/docs
docs.title=GORM + PostgreSQL API
docs.version=1.0.0

# Database — added by: keel add gorm
database.engine=${DATABASE_ENGINE:postgres}
database.url=${DATABASE_URL:postgres://postgres:postgres@localhost:5432/keelexamples?sslmode=disable}
