# Application
app.name=${SERVICE_NAME:middleware-example}
app.env=${APP_ENV:development}
server.port=${PORT:7331}

# API docs
docs.path=/docs
docs.title=Middleware API
docs.version=1.0.0

# Middleware settings
middleware.internal-key=${INTERNAL_KEY:dev-internal-key}
middleware.blocked-ips=${BLOCKED_IPS:}
