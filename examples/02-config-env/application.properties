# Application
app.name=${SERVICE_NAME:config-env}
app.env=${APP_ENV:development}
server.port=${PORT:7331}

# API docs
docs.path=/docs
docs.title=Config & Env API
docs.version=1.0.0

# Feature flags & tuning
app.api-version=${API_VERSION:v1}
app.log-level=${LOG_LEVEL:info}
app.max-page-size=${MAX_PAGE_SIZE:50}
app.feature-flag=${FEATURE_FLAG:false}
