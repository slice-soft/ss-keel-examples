# Application
app.name=${SERVICE_NAME:mongo-example}
app.env=${APP_ENV:development}
server.port=${PORT:7331}

# API docs
docs.path=/docs
docs.title=MongoDB API
docs.version=1.0.0

# MongoDB — added by: keel add mongo
mongo.uri=${MONGO_URI:mongodb://localhost:27017}
mongo.database=${MONGO_DATABASE:keelexamples}
