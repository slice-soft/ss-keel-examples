# Application
app.name=${SERVICE_NAME:jwt-addon}
app.env=${APP_ENV:development}
server.port=${PORT:7331}

# API docs
docs.path=/docs
docs.title=JWT Addon API
docs.version=1.0.0

# JWT — added by: keel add jwt
jwt.secret=${JWT_SECRET:change-me-in-production}
jwt.issuer=${JWT_ISSUER:}
jwt.token-ttl-hours=${JWT_TOKEN_TTL_HOURS:24}
