# Application
app.name=${SERVICE_NAME:redis-cache-example}
app.env=${APP_ENV:development}
server.port=${PORT:7331}

# API docs
docs.path=/docs
docs.title=Redis Cache API
docs.version=1.0.0

# Redis — added by: keel add redis
redis.url=${REDIS_URL:redis://localhost:6379}

# Cache settings
cache.ttl-seconds=${CACHE_TTL_SECONDS:30}
