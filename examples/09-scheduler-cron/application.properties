# Application
app.name=${SERVICE_NAME:scheduler-cron}
app.env=${APP_ENV:development}
server.port=${PORT:7331}

# API docs
docs.path=/docs
docs.title=Scheduler / Cron API
docs.version=1.0.0

# Scheduler settings
scheduler.heartbeat-interval=${HEARTBEAT_INTERVAL:10s}
scheduler.cleanup-interval=${CLEANUP_INTERVAL:1m}
