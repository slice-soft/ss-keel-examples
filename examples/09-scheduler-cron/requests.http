@base = http://localhost:7331

### Job execution history (poll this while the server runs to see cron entries)
GET {{base}}/jobs/history

### Health check
GET {{base}}/health

### OpenAPI docs (browser)
GET {{base}}/docs
